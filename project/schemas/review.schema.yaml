---
# Review schema.
# IMPORTANT: Review completion does NOT constitute Founder acceptance.
# A complete review documents that review occurred and states findings.
# Final acceptance authority remains with the designated human authority.
"$schema": "https://json-schema.org/draft/2020-12/schema"
"$id": "asa2/pos/review"
title: Review
type: object
required:
  - id
  - subject_type
  - subject_id
  - work_item_id
  - reviewer
  - reviewer_role
  - status
  - scope
  - findings
  - evidence_reviewed
  - conclusion
  - created_at
  - completed_at
properties:
  id:
    type: string
    pattern: "^ASA2-REVIEW-[0-9]+"
  subject_type:
    type: string
  subject_id:
    type: string
  work_item_id:
    type: string
  reviewer:
    type: string
  reviewer_role:
    type: string
  status:
    type: string
    enum:
      - requested
      - in_progress
      - changes_requested
      - complete
      - cancelled
  scope:
    type: string
    minLength: 1
  findings:
    type: array
    items:
      type: object
      additionalProperties: true
  evidence_reviewed:
    type: array
    items:
      type: string
  conclusion:
    type: [string, "null"]
  created_at:
    type: string
    format: date-time
  completed_at:
    type: [string, "null"]
    format: date-time
additionalProperties: false
