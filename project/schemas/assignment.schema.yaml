---
"$schema": "https://json-schema.org/draft/2020-12/schema"
"$id": "asa2/pos/assignment"
title: Assignment
type: object
required:
  - id
  - work_item_id
  - risk_record
  - worker_type
  - status
  - base_commit
  - objective
  - allowed_scope
  - forbidden_scope
  - required_tests
  - required_outputs
  - result_path
  - assigned_by
  - acceptance_authority
  - created_at
  - updated_at
properties:
  id:
    type: string
    pattern: "^ASA2-ASG-[0-9]+"
  work_item_id:
    type: string
  risk_record:
    type: [string, "null"]
    description: "ID of the risk record for this assignment's work item"
  worker_type:
    type: string
  status:
    type: string
    enum:
      - draft
      - issued
      - acknowledged
      - in_progress
      - submitted
      - closed
      - cancelled
  base_commit:
    type: string
    minLength: 1
  objective:
    type: string
    minLength: 1
  allowed_scope:
    type: array
    items:
      type: string
    minItems: 1
  forbidden_scope:
    type: array
    items:
      type: string
    minItems: 1
  required_tests:
    type: array
    items:
      type: string
  required_outputs:
    type: array
    items:
      type: string
  result_path:
    type: [string, "null"]
  assigned_by:
    type: string
  acceptance_authority:
    type: string
  created_at:
    type: string
    format: date-time
  updated_at:
    type: string
    format: date-time
additionalProperties: false
